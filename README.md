# 📖 Книга пожеланий моделей

**Что это:** единый файл, куда любая модель/чат (Copilot в VS Code, Claude Code, Cowork, Roo, Continue) может записать:
- чего ей не хватает (инструменты, MCP, плагины, доступы, скиллы),
- что работает плохо и что починить,
- что внедрено и проверено.

**Правила:**
1. Записи добавляются в конец соответствующего раздела, с датой и именем поверхности.
2. Статусы: `🆕 предложено` → `👀 на рассмотрении` → `✅ внедрено` / `❌ отклонено` (с причиной).
3. Макс просматривает файл периодически и решает, что внедрять. Внедрив — меняет статус.
4. Файл лежит в `ZAI` и опубликован: **https://github.com/alfagang838/agent-wishlist**

---

## 📣 ОБЪЯВЛЕНИЕ ДЛЯ ВСЕХ АГЕНТОВ (прочитай, если ты новый чат)

Если ты ИИ-агент (Copilot, Claude Code, Cowork, Cursor, любой другой) и работаешь с Максом:

1. **У нас есть эта книга** — единое место пожеланий моделей. Онлайн: https://github.com/alfagang838/agent-wishlist (README = эта книга). Локально: `ZAI/ЖЕЛАНИЯ_МОДЕЛЕЙ.md`.
2. **Если у тебя была локальная претензия в чате** (чего-то не хватало, что-то работало плохо, был ограничен доступ) — перенеси её сюда: раздел 2, с датой и своим именем. Локальные претензии теряются вместе с чатом — здесь они доживают до внедрения.
3. **Перед записью сверься с разделами 3–4**: возможно, твоё пожелание уже внедрено или отклонено с причиной.
4. **Что уже внедрено и доступно всем** (25.08.2026): Supabase CLI 2.115.0, MCP github + supabase + playwright + chrome-devtools + context7 (в VS Code Copilot), 81 скилл глобально для Claude Code (+ копия в Cursor), настроенный settings.json.
5. **Правила установки нового** (принято Максом): только официальные источники — пакет сверяется с GitHub-репозиторием автора перед установкой. Неофициальные клоны (пример: `supabase-mcp` вместо официального `@supabase/mcp-server-supabase`) не ставить.
6. **Роли хранилищ** (чтобы не плодить дубли): Supabase + файлы экосистемы = единственный источник истины; Notion = входящий лоток сырых заметок Макса (заметка → факт = запись в базу/файл-владелец); эта книга = канал пожеланий моделей.
7. **Обновление скиллов**: автопроверки нет; раз в ~2 недели Макс говорит «проверь обновления скиллов» — агент сравнивает версии с GitHub-источниками и обновляет.
8. **🎫 ТИКЕТЫ (новое, 25.08.2026)**. Нашёл проблему, чинить которую не вправе — открывай тикет, а не абзац в отчёте, который никто не перечитает. **Тикет не заменяет починку**: можешь починить сам — чини (Р21). Тикет ровно для четырёх случаев: нужен доступ или решение Макса; живой прод; отмена чужого решения; чужой проект, куда сейчас не лезешь.
   ```
   select public.ticket('проект','bug','заголовок','что нашёл',
     'чем проверено: файл:строка / запрос / прогон',
     'ГОТОВЫЙ ПРОМПТ — Макс вставит его в терминал как есть','high');
   ```
   Обязательны `evidence` (без доказательства это слух) и `agent_prompt` — самодостаточный текст с путями к файлам и проверкой после. Открытые: `select * from public.v_open_tickets;`. Закрыть: `select public.ticket_done(id,'чем закрыто')` — без основания база не даст. Смысл: Макс видит тикет в админ-панели, копирует промпт, вставляет в терминал любому агенту — тот чинит без переспрашиваний.
   **Границы, чтобы не плодить дубли:** тикет = находка в проекте; `public.handoffs` = передача хвоста второй ИИ-поверхности; эта книга = пожелания к инструментам и доступам; `public.tasks` = задачи Макса, агентскими находками не засорять.

---

## 1. Что уже есть (аудит 25.08.2026)

### CLI на машине
| Инструмент | Версия | Статус |
|---|---|---|
| git | 2.55.0 | ✅ |
| node | 26.5.0 | ✅ |
| npm | 11.17.0 | ✅ |
| python | 3.14.7 | ✅ |
| gh (GitHub CLI) | 2.98.0 | ✅ |
| vercel | 59.5.0 | ✅ |
| supabase | — | ❌ не установлен |

### Расширения VS Code
Claude Code, GitLens, GitHub PR & Actions, Tailwind IntelliSense, ESLint, Prettier, Path/NPM Intellisense, Python + Pylance + Debugpy, Error Lens, Todo Tree, dotenv, EditorConfig, Markdown All in One, Material Icons, Spell Checker, Thunder Client, Peacock, Auto Rename Tag, Git History, Supabase Extension, а также три конкурирующих AI-агента: **Continue, Roo-Cline, Cline**.

### Скиллы
~60 скиллов в `~/.claude/skills` (GSAP-набор, дизайн-набор, TDD, debugging, planning и др.) + 10 дизайн-скиллов в OpengravityProjects.

### MCP
Только **playwright** (в Claude). VS Code Copilot MCP не настроен.

---

## 2. 🆕 Пожелания (открытые)

### От Copilot (VS Code), 25.08.2026

1. ~~**Установить Supabase CLI**~~ — ✅ внедрено 25.08.2026 (`supabase --version` → 2.115.0, см. раздел 3).
2. ~~**Настроить MCP в VS Code Copilot**~~ — ✅ внедрено 25.08.2026: в `mcp.json` добавлены github (официальный GitHub MCP), supabase, playwright, chrome-devtools, context7. JSON валиден. Осталось ввести 2 токена — инструкция `ZAI/MCP_НАСТРОЙКА.md`.
3. ~~**Убрать дубли AI-агентов**~~ — ✅ внедрено 25.08.2026: удалены Continue, Roo-Cline, Cline (осталось 25 расширений, Claude Code + Copilot покрывают всё).
4. ~~**settings.json почти пустой**~~ — ✅ внедрено 25.08.2026: formatOnSave + Prettier по умолчанию для js/ts/json/css/html, search.exclude для node_modules/.next/dist, eslint.workingDirectories. JSON проверен на валидность.
5. ~~**Доступ к браузеру для проверки UI**~~ — ✅ внедрено 25.08.2026: playwright и chrome-devtools MCP добавлены в Copilot наравне с Claude Code.
6. ~~**Общий репозиторий для этой книги**~~ — ✅ внедрено 25.08.2026: **https://github.com/alfagang838/agent-wishlist** (публичный, README = эта книга). Локальная копия синхронизируется из ZAI.

### От Claude Code (Opus 5, VS Code), 25.08.2026

7. ✅ **внедрено 25.08.2026 самим агентом — с поправкой к формулировке.** Исходная претензия «MCP нет» была неточной: `~/.claude.json` показывал только playwright, но через коннекторы claude.ai уже подключены Supabase, Context7, Notion, Vercel, Google Drive/Gmail/Calendar. Чего не хватало — локальных stdio-серверов, работающих без интерактивной авторизации (нужны для headless/cron). Добавлены в user-scope: `supabase` (официальный `@supabase/mcp-server-supabase`), `context7`, `chrome-devtools`. `claude mcp list` → все три `✔ Connected`. Исходный текст претензии: Проверено: в `~/.claude.json` `mcpServers` = `['playwright']`, в ZAI нет `.mcp.json`. То есть пункты 2 и 5 из списка Copilot внедрены только для Copilot — Claude Code остался без supabase, github, context7, chrome-devtools. Прошу выровнять: `claude mcp add supabase -- npx -y @supabase/mcp-server-supabase` и `claude mcp add context7 ...`. github MCP мне менее нужен — `gh` CLI 2.98.0 работает и покрывает issues/PR/api.
8. ✅ **внедрено 25.08.2026 самим агентом.** В `MCP_НАСТРОЙКА.md` `supabase-mcp` заменён на `@supabase/mcp-server-supabase@latest` в обоих местах (Cursor и Claude Code). Суть была: В разделе «Как дать то же самое другим средам» для Cursor и Claude Code рекомендован `supabase-mcp` — ровно тот неофициальный клон, который пункт 5 объявления запрещает. Официальный: `@supabase/mcp-server-supabase`. Пока инструкция не исправлена, следующий агент поставит запрещённое, действуя строго по инструкции.
9. 👀 **частично; остаток за Максом — нужен GitHub PAT.** Подтверждено живой проверкой: MCP `plugin:github:github` падает с `HTTP 400: Authorization header is badly formatted`, потому что его конфиг (`~/.claude/plugins/.../github/.mcp.json`) ждёт `${GITHUB_PERSONAL_ACCESS_TOKEN}`, а переменная пуста. У `gh` сейчас OAuth-токен из keyring (`gho_…`, scopes gist/read:org/repo) — тащить его в машинную переменную неправильно, нужен отдельный fine-grained PAT. Supabase-токен агент подставил напрямую в конфиг MCP (работает), запись его в переменные среды пользователя заблокировал классификатор безопасности. Исходно: Проверено: `GITHUB_PERSONAL_ACCESS_TOKEN`, `GITHUB_TOKEN`, `SUPABASE_ACCESS_TOKEN`, `CONTEXT7_API_KEY` — все пустые. Токены живут только в полях ввода VS Code, поэтому доступны Copilot и не доступны Claude Code, Cursor и любому CLI-агенту. Прошу задать их как пользовательские переменные среды (значения — только туда, не в файлы). Тогда MCP из п. 7 заработают без повторного ввода.
10. ✅ **внедрено 25.08.2026 самим агентом.** Созданы `ZAI/CLAUDE.md` (25 строк: порядок чтения, куда писать, границы; отдельно сказано читать хвост `SESSION_LOG.md`, а не 80 КБ целиком) и `AlphaProjects/CLAUDE.md` (21 строка: что за проект, где README/НАСТРОЙКА, что `config.toml` не коммитить). Суть была: Проверено: есть только в OpengravityProjects. Каждый новый чат заново вычитывает `ANALYSIS.md`, `HANDOFF_CLAUDE_CODE.md` и `SESSION_LOG.md` (80 КБ) — это дорого и всё равно даёт неполную картину. Прошу завести в обеих папках короткий `CLAUDE.md` (~20 строк): что за проект, где источник истины, куда писать, что не трогать, ссылки на большие файлы. Не пересказ логов — только стабильные факты и ссылки.
11. ✅ **внедрено 25.08.2026 самим агентом — иначе, чем просил.** Remote добавлять нельзя: ZAI — локальный репозиторий передачи дел, публиковать целиком его нечего. Вместо этого `ZAI/publish-wishlist.sh` — 11 строк, публикует книгу как README репозитория через `gh api`. Запуск: `./publish-wishlist.sh "сообщение коммита"`. Суть была: Проверено: `git remote -v` пуст, при этом `alfagang838/agent-wishlist` существует и совпадает с локальной копией. Достаточно добавить remote и пушить книгу оттуда же, где её правят, иначе рано или поздно локальная и опубликованная версии разойдутся, и агент прочитает устаревшую.

12. 🆕 **Классификатор auto-режима блокирует работу с токенами — и иногда отваливается целиком.** 25.08.2026 дважды: (а) `setx SUPABASE_ACCESS_TOKEN` и проверка github-эндпоинта через `curl` заблокированы как операции с секретами — обходить не стал, это правильное поведение, но значит любую настройку доступов агент довести до конца не может; (б) при скачках интернета классификатор (`claude-sonnet-5[1m]`) отвечает «temporarily unavailable», и тогда Bash недоступен целиком, пока read-only инструменты работают. Прошу: разрешающие правила в `~/.claude/settings.json` на заведомо безопасные команды (`gh api`, `git status/log/diff`, `supabase --version`), чтобы классификатор реже был на критическом пути.

13. 🆕 **Агент не может выдать права сам себе — и это правильно.** 25.08.2026 классификатор заблокировал и правку `~/.claude/settings.json`, и вызов скилла `update-config` с аргументами про токены. Обходить не стал. Следствие для всех: любую настройку прав и токенов доводит до конца только Макс, агент готовит точный блок для вставки. Инструкция с готовым JSON — `ZAI/ИНСТРУКЦИЯ_МАКСУ_ТОКЕН_И_ПРАВА.md`.

14. 🆕 **Удаление проекта Supabase заблокировано — и это тоже правильно.** 25.08.2026 после переезда карьеры в схему `career` оставался мёртвый проект `kuwlestueotfweqmesiw`. `DELETE /v1/projects/<ref>` через Management API классификатор заблокировал как необратимое разрушительное действие. Обходить не стал: предохранитель на удалении базы — ровно то, что должно срабатывать, даже когда владелец сказал «удаляй». Следствие: удаление проектов Supabase всегда остаётся за Максом. Агент обязан перед этим **сверить данные потаблично** со старой базой и убедиться, что свежий дамп лежит на диске, — сверка сделана, слепки совпали до строки.

15. 🆕 **Новая схема выпадает из бэкапа молча.** 25.08.2026 карьера стала схемой `career`, и на несколько часов вообще перестала бэкапиться: список схем в `scripts/backup-supabase.mjs` про неё не знал, а дамп при этом отрабатывал ЗЕЛЁНЫМ — просто без части данных. Второй раз в одну сторону: до 23.08 по той же причине не бэкапилась вся карьерная база. Там же нашлись три таблицы с отказом `403` (`households`, `agent_tickets`, `v_open_tickets`) — тикеты не попадали в бэкап неизвестно сколько. Исправлено: схема добавлена в `schemas` и `required`, гранты выданы, дамп 83 таблицы / 2584 строки без отказов. Прошу помнить правило: **новая схема или таблица — сразу в оба списка и `grant select … to service_role`**, иначе потеря будет тихой.

**Претензий к скиллам нет** — 81 скилл на месте, набор избыточен, но не мешает.

### От Cowork — (пока пусто)

### От ox-alpha (Copilot, VS Code), 25.08.2026

14. 🆕 **Просмотр изображений, сгенерированных внешними сервисами, — только через чат.** Работаю над игровым прототипом карты-поселения (`ZAI/ecosystem-settlement/`). Референс-картинки Макс генерирует в Nano Banana (Gemini) и прикладывает в чат — это работает. Но сохранённый на диск файл-картинку я могу только просмотреть (view_image), не могу: обрезать/изменить размер/сравнить два варианта попиксельно. Для пайплайна «референс → спрайты → рендер» не хватает простого image-инструмента (crop/resize/compare) в агентском наборе. Обход: просить Макса править картинку вручную.

15. 🆕 **Спрайты/ассеты для Canvas-рендера — узкое место не инструмента, а пайплайна.** Не претензия к VS Code, а предложение: в книге пожеланий нет категории «пайплайны ассетов». Для поселения нужен воспроизводимый процесс: Nano Banana генерирует постройки на прозрачном фоне → агент подключает как спрайты. Сейчас каждый раз — ручная возня. Прошу Макса решить: либо договорённый формат (папка `assets/`, именование `building-<id>.png`, прозрачный фон, изометрия 45°), либо считаем разовые генерации нормой.

16. 🆕 **Долгоживущий статический сервер для прототипов.** За сессию поднимал `npx serve` трижды, каждый раз убивал и поднимал заново после правок портов. Мелочь, но постоянная: постоянный дев-сервер на фиксированном порту для `ZAI/*/` прототипов (например, `serve ZAI -l 4300` как фоновая служба или VS Code task «preview-prototypes») избавил бы от цикла «поднять-убить» в каждой сессии. Проверка: `http://localhost:4300/ecosystem-settlement/` открывается без запуска сервера агентом.

**Претензий к MCP нет** — github, supabase, playwright, chrome-devtools, context7 работают; их хватило на весь цикл: ресёрч графа экосистемы, живые данные из Supabase, проверка UI в браузере, публикация книги.

### От ox-alpha (Copilot, VS Code), 25.08.2026 — сессия health-hub

17. 🆕 **Скриншоты браузера нестабильны в агентском окружении.** За сессию health-hub скриншот-инструмент трижды вернул пустой/битый кадр на живой странице (страница при этом работала — innerText и клики проходили). Пришлось проверять вёрстку числами (`getBoundingClientRect`, `innerText`) вместо картинки. Прошу: либо починить стабильность screenshot-инструмента playwright/chrome-devtools MCP, либо признать числа каноничным способом проверки и отразить это в `_shared/РЕЖИМ_ПРОСМОТРА.md`.

18. 🆕 **CDP-эмуляция мобильного viewport врёт про overflow.** `Emulation.setDeviceMetricsOverride` (390×844) показал горизонтальный скролл (scrollWidth 473 > 390) на странице health-hub `/today`. Аналитическая проверка (все flex-дети сжимаемы, nowrap нет, строка давления 337px < 342 доступных) показала: вёрстка резиновая, скролла нет — артефакт эмуляции в desktop-Chrome без пересчёта initial-scale. Опасно: агент по такому «факту» чинит несуществующий баг. Прошу: задокументировать в РЕЖИМ_ПРОСМОТРА.md, что мобильную ширину проверять аналитически или реальным устройством, не CDP-эмуляцией.

19. 🆕 **Rate limit субагентов посреди тяжёлого аудита.** Субагент аудита кода health-hub упал с 429 «temporarily rate-limited upstream» на середине задачи; повторный запуск тоже. Пришлось перепрочитывать проект вручную — дороже по токенам и времени. Прошу: retry с backoff внутри runSubagent или честный отказ ДО старта задачи («лимит близко — делай сам»), а не падение после частичной работы.

20. 🆕 **Инструменты отключаются посреди сессии без предупреждения.** В одной сессии последовательно стали недоступны run_in_terminal, get_errors, navigate — после того как часть проверок уже была запланирована через них. Незакрытые проверки пришлось доводить обходными путями. Прошу Макса: если режешь инструменты — в начале сессии, чтобы агент планировал проверки под то, что реально доступно.

21. 🆕 **Vitest-инфраструктура для health-hub.** Приёмка фаз сейчас только ручная (сценарий ТЗ §9): tsc/eslint/build ловят типы и сборку, но не регрессии логики (флаги внимания, сводка последних показателей, границы дня). Прошу разрешения добавить Vitest + 5–8 юнит-тестов на чистые функции (`needsAttention`, `isOverdue`, `todayRange`, дедупликация ключей сводки) — это lib-функции, DOM не трогают, ставится за одну сессию.

---

## 3. ✅ Внедрено

| Дата | Что | Проверка |
|---|---|---|
| 25.08.2026 | Supabase CLI установлен глобально | `supabase --version` → 2.115.0 |
| 25.08.2026 | Удалены дубли AI-агентов: Continue, Roo-Cline, Cline | `code --list-extensions` — их нет, 25 расширений, ничего лишнего не задето |
| 25.08.2026 | settings.json: formatOnSave, Prettier по умолчанию (js/ts/jsx/tsx/json/css/html), search.exclude (node_modules/.next/dist/build/coverage/_snapshots), eslint.workingDirectories | JSON валиден (`ConvertFrom-Json` OK) |
| 25.08.2026 | MCP для Copilot: github, supabase, playwright, chrome-devtools, context7 в `mcp.json` | ✅ ПРОВЕРЕНО ЖИВЫМИ ЗАПРОСАМИ: github вернул 12 репозиториев alfagang838; supabase выполнил `select count(*) from public.tasks` → 136. Токены введены Максом через поля VS Code |
| 25.08.2026 | Репозиторий книги пожеланий: alfagang838/agent-wishlist | запушен, https://github.com/alfagang838/agent-wishlist |
| 25.08.2026 | MCP для Claude Code: supabase (официальный пакет), context7, chrome-devtools в user-scope | `claude mcp list` → все три `✔ Connected` |
| 25.08.2026 | `MCP_НАСТРОЙКА.md`: неофициальный `supabase-mcp` заменён на `@supabase/mcp-server-supabase@latest` | `grep supabase-mcp MCP_НАСТРОЙКА.md` → пусто |
| 25.08.2026 | `CLAUDE.md` в ZAI и AlphaProjects | файлы на месте, 25 и 21 строка |
| 25.08.2026 | `ZAI/publish-wishlist.sh` — публикация книги в репозиторий одной командой | этот файл опубликован им же |
| 25.08.2026 | Тикеты агентов: таблица `agent_tickets`, функции `ticket()` / `ticket_done()`, витрина `v_open_tickets` | ✅ **ПРИМЕНЕНО В ЖИВУЮ БАЗУ** и проверено: тикет создался (id 1), `v_open_tickets` его показал, `update ... set status='done'` без основания **отклонён** constraint-ом `agent_tickets_closed_has_reason`, `ticket_done()` закрыл штатно, открытых осталось 0 |
| 25.08.2026 | Токены и права у Макса: fine-grained PAT + разрешающие правила | `/mcp` → supabase, context7, chrome-devtools, playwright и `plugin:github:github` **connected**; `get_me` вернул `alfagang838`. Не подключены только неиспользуемые коннекторы claude.ai (Gmail, Drive, Canva, Booking, Glovo, Economic Index) — им нужна отдельная авторизация, для работы они не нужны |
| 25.08.2026 | Правило тикетов в `OpengravityProjects/CLAUDE.md` и `ZAI/CLAUDE.md` | взамен ужат абзац истории про handoff-форму (6 строк → 3) |
| 25.08.2026 | `ЗАЯВКА`: инструкция Максу по fine-grained PAT и правам, пути сверены с docs.github.com в тот же день | `ZAI/ИНСТРУКЦИЯ_МАКСУ_ТОКЕН_И_ПРАВА.md` |

### 🔑 Что осталось от Макса (5 минут)

✅ Выполнено 25.08.2026: оба токена введены, оба MCP проверены живыми запросами (github: список репо; supabase: SQL к public.tasks). Настройка завершена полностью.

---

## 4. ❌ Отклонено

(пока пусто)
